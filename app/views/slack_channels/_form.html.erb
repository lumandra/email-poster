<%= form_for([slack_channel.report, slack_channel]) do |f| %>
  <% if slack_channel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(slack_channel.errors.count, "error") %> prohibited this slack_channel from being saved:</h2>

      <ul>
      <% slack_channel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :report_id %>

  <div class="form-group">
    <%= f.label :slack_message_text %>
    <div class="input-group col-md-6">
      <%= f.text_field :slack_message_text, autocomplete: "off", class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :webhook_url %>
    <div class="input-group col-md-6">
      <%= f.text_field :webhook_url, autocomplete: "off", class: "form-control" %>
    </div>
  </div>

  <div class="actions">
    <%= f.submit 'Submit', class: "btn btn-success" %>
  </div>
<% end %>
